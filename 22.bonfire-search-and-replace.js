function replace(str, find, replace){  
   return str.replace(new RegExp(find, 'gi'), replace);  
  var posBefore = str.indexOf(before);
  if (posBefore charat){
     uppercase
     lowercase

  }
}

replace("A quick brown fox jumped over the lazy dog", "Jumped", "leaped");
